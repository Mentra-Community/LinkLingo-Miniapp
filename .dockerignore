.git
node_modules
**/node_modules

.env
.env.local
.env.*.local

# miniapp/ IS part of the build context: the image bakes the built bundle and
# serves it at /miniapp. Only local build output is excluded so a stale laptop
# build can never ship inside the image.
miniapp/dist
miniapp/build

*.md
.github
.vscode
.idea
*.zip
.mentra
.DS_Store
